These instructions assume you have Go install.
Before you run install dependencies `go install`
To run `go run .`
To stop control C
To change port modify PORT_NUMBER in server.go

Note: Tests assume server is running locally